import 'package:flutter/material.dart';

import '../../widgets/title_screen.dart';

class LoadingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return TitleScreen(Container());
  }
}
